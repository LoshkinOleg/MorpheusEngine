- SessionStoreModule:
  - Make UI to inspect last turn's world_state and view_state.
  
- Use qwen 3.5

- Check if we're using memGPT's heartbeat approach to tool calling / deciding when to return control to player. If we are, simplify: generate_prose tool call is inherently terminal while all other tool calls are ones where the LLM is expected to continue generating.
- Check if we're using memGPT's tagging for archival memory and remove it if it's used.

- UI: once engine is initialized, display "ready!"

- Make intent extractor actions data driven instead of hardcoded.

- Investigate good options to use with qwen: {
  "model": "llama3.2",
  "prompt": "your prompt here",
  "stream": false,
  "options": {
    "temperature": 0.2,
    "top_k": 40,
    "top_p": 0.9
  }
}

- Play with ollama thinking parameters

- Look into mem0's entity boost and into lemmatization.

- PlotMaster module:
  - called regularly by the Director to consolidate the new story diff:
    - upsert a global plot document, hidden from the player, to explain most of the inconsistencies that have happened.
    - The Director should then be able to rely on that plot document to decide where to move the narrative in the following answers to prompts.

- implement tool capabilities for the LLM provider

- change what the LLM sees to be formated more like so:
```
<instructions>
You are the GM for an ongoing solo tabletop RPG...
</instructions>

<lore>
boarding: A common tactic in crawler-versus-crawler combat...
city_state_politics: Politics are strictly local...
</lore>
```