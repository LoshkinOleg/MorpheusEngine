- start implementing tests
- don't clarify intent for now, only decide to refuse an action but give the player an explanation as to why.
- Implement intent clarification pipeline.
- Implement anchors
- Fix front end fetching backend before it's ready
- Implement lore accumulator mechanism for the proser OK?
- Make test to ensure that multiple intent candiates can be returned by the intent extractor and that all of them are considered in the follow up pipeline.
