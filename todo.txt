- Make context inspector:
  - Main context inspector OK
  - Main context inspector: toggle human friendly formatting, keep same source string
  - External context inspector: archival store
  - External context inspector: recall store
  - render the exact string sent to the LLM by inspecting ollama logs

- EngineHttpContracts.cs: I don't like how everything is a string, makes reading the type difficult.

- Right now there seems to be two token budget estimation system coexisting. This is confusing, simplify and keep only one.

- "On a later step of the same turn, recall_search / archival_memory_search results are persisted (persist_step) and
then show up in recentMessages on the next load_context. They are not injected into the same compile pass before
that persist.": we should eliminate these persist - recall calls at each step, we should be able to deduce the current
state from (state at turn start) + (accumulated diffs from each step this turn). Then only at the end of the turn should
we persist to session store. Avoids needless hops to the session store.

- I think right now memories can be mutated by the LLM. Looking at some other orchestrations, people seem to prefer append only changed due to lack
of reliability of memory editing by the LLM.

- SessionStoreModule:
  - Make UI to inspect last turn's world_state and view_state.
  
- Use qwen3:4b-instruct-2507-q4_K_M

- Tokenizer: "The gist is three steps:

**Grab the tokenizer file** from the HuggingFace repo `Qwen/Qwen3-4B-Instruct-2507`. You only need `tokenizer.json` — it's the self-contained BPE definition (vocab + merge rules + special tokens all in one). Commit it into your MorpheusEngine repo as a bundled asset, something like `Resources/Tokenizers/qwen3-4b-instruct-2507.json`.

**Add Microsoft.ML.Tokenizers** via NuGet. The package can load HuggingFace-format `tokenizer.json` files directly. You'd initialize it once at engine startup, roughly: `Tokenizer.CreateTiktokenForEncoding(...)` — actually for HuggingFace BPE it's more like `Tokenizer.CreateBpe(...)` or loading from the JSON. You'll want to verify the exact API surface since this library has been evolving; check the latest docs. The key method you care about is `CountTokens(string text)` or `EncodeToIds(string text).Count`.

**Wire it into your turn pipeline.** Create a thin `ITokenCounter` interface with a `int CountTokens(string text)` method, backed by the ML.Tokenizers implementation. Use it in your context assembly phase — as you pack system prompt, memory blocks, conversation history, and tool schemas into the working context, you call `CountTokens` on each segment and track the running total against your budget (model's context length minus reserved output tokens). This also lets you swap tokenizers later if you switch models, without touching the pipeline logic.

One thing to validate early: after you've got it loaded, send a few test strings through both your local tokenizer and an actual Ollama inference call (the response includes `prompt_eval_count` which tells you how many tokens Ollama saw in the prompt). Make sure the counts match — if they're off by a few, it's likely the chat template adding special tokens that you're not accounting for in your raw string count."

- Check if we're using memGPT's heartbeat approach to tool calling / deciding when to return control to player. If we are, simplify: generate_prose tool call is inherently terminal while all other tool calls are ones where the LLM is expected to continue generating.
- Check if we're using memGPT's tagging for archival memory and remove it if it's used.

- UI: once engine is initialized, display "ready!"

- Make intent extractor actions data driven instead of hardcoded.

- Investigate good options to use with qwen: {
  "model": "llama3.2",
  "prompt": "your prompt here",
  "stream": false,
  "options": {
    "temperature": 0.2,
    "top_k": 40,
    "top_p": 0.9
  }
}

- Play with ollama thinking parameters

- Look into mem0's entity boost and into lemmatization.

- PlotMaster module:
  - called regularly by the Director to consolidate the new story diff:
    - upsert a global plot document, hidden from the player, to explain most of the inconsistencies that have happened.
    - The Director should then be able to rely on that plot document to decide where to move the narrative in the following answers to prompts.

- implement tool capabilities for the LLM provider

- change what the LLM sees to be formated more like so:
```
<instructions>
You are the GM for an ongoing solo tabletop RPG...
</instructions>

<lore>
boarding: A common tactic in crawler-versus-crawler combat...
city_state_politics: Politics are strictly local...
</lore>
```